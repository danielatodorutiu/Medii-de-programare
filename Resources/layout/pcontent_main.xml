<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/puser_app_bar_main">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:id="@+id/background_picture_map"
        android:background="@mipmap/background_app"
        />

     <include layout="@layout/puseraccountinfo"
             android:visibility="gone"
             android:id="@+id/layout_pUserAccountInfo"/>

        <include layout="@layout/pusermanagehoney"
             android:visibility="gone"
             android:id="@+id/layout_pUserManageHoney"/>

      
     <include layout="@layout/puseraccountsettings"
             android:visibility="gone"
             android:id="@+id/layout_pUserAccountSettings"/>

</RelativeLayout>
